{
  "name": "monorepo",
  "version": "0.0.0",
  "private": true,
  "homepage": "https://prajal-gautam.github.io/Portfolio-New",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d apps/web/dist"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "gh-pages": "^6.3.0"
  },
  "workspaces": [
    "apps/*"
  ]
}
